# Copy to .env.local for local development. Points the frontend at the
# separately-running backend dev server. Leave unset (or empty string) in a
# production build, where the frontend is served by the same service as the
# API and relative "/api/..." paths are used instead (see lib/api.ts).
NEXT_PUBLIC_API_URL=http://localhost:8010
